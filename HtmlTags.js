function area(obj, text) {
    obj.innerText = text;
}
function base(obj, text) {
    obj.innerText = text;
}
function basefont(obj, text) {
    obj.innerText = text;
}
function br(obj, text) {
    obj.innerText = text;
}
function col(obj, text) {
    obj.innerText = text;
}
function frame(obj, text) {
    obj.innerText = text;
}
function hr(obj, text) {
    obj.innerText = text;
}
function img(obj, text) {
    obj.innerText = text;
}
function input(obj, text) {
    obj.innerText = text;
}
function link(obj, text) {
    obj.innerText = text;
}
function meta(obj, text) {
    obj.innerText = text;
}
function param(obj, text) {
    obj.innerText = text;
}
function p(obj, text) {
    obj.innerText = text;
}


